# YouTube Upload API - Environment Variables
# Copy to .env when deploying (e.g. Railway): cp .env.example .env

# Server port (Railway sets this automatically - leave as 3000 for local dev)
PORT=3000

# --- Easiest: In-app OAuth (no Playground, no tokens in n8n) ---
# Set these, then visit {BASE_URL}/auth/youtube once to connect. After that, n8n
# only needs to send videoUrl + videoMetadata - no tokens or uploadUrl.

BASE_URL=https://your-app.railway.app
YOUTUBE_CLIENT_ID=your-client-id.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=your-client-secret

# Add this redirect URI in Google Cloud Console (Credentials → your OAuth client):
#   {BASE_URL}/auth/youtube/callback

# --- API key (optional but recommended in production) ---
# If set, all /upload and /job/:id requests must include header: X-API-Key: your-key
API_KEY=your-secret-api-key
